# 截图

README 里引用的图，文件名固定，换图直接覆盖同名文件即可：

| 文件 | 内容 |
|---|---|
| `history.png` | 输入历史页 |
| `stats.png` | 统计页 |
| `meetings.png` | 会议页 |

## 待重拍

带 `-OLD` 后缀的是旧版本截图，**没有被 README 引用**：

- `settings-OLD.png` —— 里面「临时录音」和「一直录音」都显示 `Control+Shift+Space`。那是升级时两个快捷键撞车的 bug，已修（新版启动会自动迁移成 `Ctrl+Shift+D`）。装上新版重拍后改名成 `settings.png`，再在 README 里加回引用。
- `overlay-OLD.png` —— 悬浮条上还有「控制台 · 隐藏」两个悬停链接，已经去掉了。
